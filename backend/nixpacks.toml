[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt --only-binary=:all:"]

[phases.build]
cmds = ["python -m app.generate_data"]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"
